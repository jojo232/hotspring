<h1>id: <%= @user.id %> のマイページ編集詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @user.id %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @user.content %></td>
  </tr>
  <tr>
    <th>内容</th>
    <td><%= @user.status %></td>
  </tr>
</table>


<%= link_to '一覧に戻る', users_path, class: 'btn btn-light' %>
<%= link_to 'マイページを編集する', edit_user_path(@task), class: 'btn btn-light' %>


